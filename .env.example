# =============================================================================
# Cooperation Toolkit - Environment Variables Example
# =============================================================================
# 
# Copy this file to .env.local and fill in your actual values.
# Never commit .env.local to version control!
#
# For detailed documentation, see ENV_VARIABLES.md
# =============================================================================

# =============================================================================
# Firebase Configuration (Required)
# =============================================================================
# Get these values from Firebase Console: Project Settings > General > Your apps > Web app
# See: https://console.firebase.google.com/

NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key-here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef123456

# Optional: Firebase Analytics Measurement ID
# NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# =============================================================================
# AI Service Configuration (Required for AI Features)
# =============================================================================
# Choose your AI provider: 'openai' (default), 'anthropic', or 'gemini'
# AI features include: natural language task creation, review assistance, retrospectives
# See: AI_SETUP.md for detailed setup instructions

AI_PROVIDER=openai

# OpenAI Configuration (Required if AI_PROVIDER=openai)
# Get API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...
# Optional: OpenAI model (defaults to 'gpt-4o-mini')
# Available models: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-3.5-turbo
# OPENAI_MODEL=gpt-4o-mini

# Anthropic Configuration (Required if AI_PROVIDER=anthropic)
# Get API key from: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-...
# Optional: Anthropic model (defaults to 'claude-3-5-sonnet-20241022')
# Available models: claude-3-5-sonnet-20241022, claude-3-opus-20240229, claude-3-sonnet-20240229
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Google Gemini Configuration (Required if AI_PROVIDER=gemini)
# Get API key from: https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=your-gemini-api-key
# Optional: Gemini model (defaults to 'gemini-1.5-pro')
# Available models: gemini-1.5-pro, gemini-1.5-flash, gemini-pro, gemini-pro-vision
# GEMINI_MODEL=gemini-1.5-pro

# Legacy: Generic AI API Key (for backward compatibility)
# Used if provider-specific key is not found
# AI_API_KEY=sk-...

# =============================================================================
# Application URL (Required for Slack Notifications)
# =============================================================================
# This URL is used in Slack notification action buttons and links
# For production: use your deployed Vercel URL
# For local development: use http://localhost:3000

NEXT_PUBLIC_APP_URL=http://localhost:3000
# Production example:
# NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# =============================================================================
# Firebase Emulator Configuration (Development Only)
# =============================================================================
# Set to 'true' to use Firebase emulators for local development
# See: https://firebase.google.com/docs/emulator-suite
# Note: Only enable during local development, disable for production

# NEXT_PUBLIC_FIREBASE_USE_EMULATOR=false
# NEXT_PUBLIC_FIREBASE_FUNCTIONS_EMULATOR_HOST=localhost
# NEXT_PUBLIC_FIREBASE_FUNCTIONS_EMULATOR_PORT=5001

# =============================================================================
# Firebase Functions Environment Variables
# =============================================================================
# IMPORTANT: These variables are NOT set in .env.local
# They are set in Firebase Functions using: firebase functions:config:set
# See ENV_VARIABLES.md for detailed instructions
#
# To set Slack configuration:
#   firebase functions:config:set slack.signing_secret="..." slack.bot_token="..."
#
# To set GitHub configuration:
#   firebase functions:config:set github.webhook_secret="..."
#
# To view current configuration:
#   firebase functions:config:get
#
# =============================================================================
# Slack Integration (Required for Slack Bot Features)
# =============================================================================
# Get these from your Slack App settings:
# - Signing Secret: Slack App > Basic Information > App Credentials > Signing Secret
# - Bot Token: Slack App > OAuth & Permissions > Bot User OAuth Token (starts with xoxb-)
#
# Set via Firebase CLI:
#   firebase functions:config:set slack.signing_secret="your-secret" slack.bot_token="xoxb-your-token"
#
# SLACK_SIGNING_SECRET=your-slack-signing-secret
# SLACK_BOT_TOKEN=xoxb-your-bot-token

# =============================================================================
# GitHub Integration (Required for GitHub Integration Features)
# =============================================================================
# Webhook Secret: Create a secure random string (e.g., openssl rand -hex 32)
# Set the same secret in both GitHub webhook settings and Firebase Functions config
#
# Set via Firebase CLI:
#   firebase functions:config:set github.webhook_secret="your-secret"
#
# GITHUB_WEBHOOK_SECRET=your-github-webhook-secret
#
# Optional: GitHub Personal Access Token (if needed for API calls)
# Get from: https://github.com/settings/tokens
# GITHUB_TOKEN=ghp_your-github-personal-access-token

# =============================================================================
# Notes
# =============================================================================
# 1. After setting variables, restart your Next.js dev server: npm run dev
# 2. For Firebase Functions, redeploy after setting config: firebase deploy --only functions
# 3. Never commit .env.local or .env to version control
# 4. Safe to commit: .env.example (this file contains no secrets)
# 5. For production deployment:
#    - Vercel: Set variables in Vercel Dashboard > Settings > Environment Variables
#    - Firebase Functions: Use firebase functions:config:set or Firebase Console
#
# For troubleshooting, see ENV_VARIABLES.md

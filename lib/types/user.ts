/**
 * Re-export types from schemas for convenience
 * Types are inferred from Zod schemas to ensure consistency
 */
export type { UserRole, UserTeams, User, UserDocument, UserUpdate } from '../schemas/user'
